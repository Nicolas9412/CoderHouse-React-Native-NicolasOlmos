import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    width: "100%",
    marginTop: 50,
    flexDirection: "row",
    justifyContent: "center",
  },
  inputText: {
    width: "70%",
    borderBottomWidth: 1,
    borderColor: "#a1c1be",
    marginRight: 10,
  },
});
